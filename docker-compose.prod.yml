version: '3.8'

services:
  app:
    build: .
    container_name: zorina-payment-app
    environment:
      - NODE_ENV=production
      - SQUARE_ENV=production
      - SQUARE_APP_ID=sq0idp-jXJquQQ4uPG0_NwR7DL86Q
      - PORT=3000
    env_file:
      - .env.production
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - payment-network

  nginx:
    image: nginx:alpine
    container_name: zorina-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
      - ./logs/nginx:/var/log/nginx
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - payment-network

networks:
  payment-network:
    driver: bridge
